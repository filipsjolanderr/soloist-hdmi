# Shared drop point between PipeWire (runs as filip) and snapserver (runs as
# _snapserver). The FIFO is pre-created here rather than by either side, so its
# ownership does not depend on which of the two starts first.
d /run/snapcast 0750 filip _snapserver -
p /run/snapcast/spotify.fifo 0640 filip _snapserver -
